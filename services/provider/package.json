{
  "name": "@deploy-gates/provider",
  "version": "0.0.0",
  "private": true,
  "description": "Custody-facade provider API (fictional, read-only)",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "dev": "tsx watch src/server.ts",
    "start": "node dist/server.js"
  },
  "dependencies": {
    "fastify": "^5.4.0"
  }
}
